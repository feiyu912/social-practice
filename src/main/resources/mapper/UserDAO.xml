<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.dao.UserDAO">
    <resultMap id="UserResultMap" type="com.ssm.entity.User">
        <id column="id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="name" property="name" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="findByUsername" parameterType="String" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" parameterType="Integer" resultMap="UserResultMap">
        SELECT * FROM user WHERE id = #{userId}
    </select>

    <insert id="insert" parameterType="com.ssm.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, email, phone, role, status, name, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{status}, #{name}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.ssm.entity.User">
        UPDATE user
        SET username = #{username},
            email = #{email},
            phone = #{phone},
            role = #{role},
            status = #{status},
            name = #{name},
            update_time = #{updateTime}
        WHERE id = #{userId}
    </update>

    <update id="updatePassword">
        UPDATE user SET password = #{password} WHERE id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{userId}
    </update>

    <delete id="delete" parameterType="Integer">
        DELETE FROM user WHERE id = #{userId}
    </delete>
    
    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM user
    </select>
    
    <select id="findByRole" parameterType="String" resultMap="UserResultMap">
        SELECT * FROM user WHERE role = #{role}
    </select>
</mapper>
